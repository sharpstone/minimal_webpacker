
task "assets:precompile" do
  puts "YOYOYOYOYO"
  puts "THE TASK ASSETS:PRECOMPILE WAS CALLED"
  
  puts "#{`which yarn`.chomp} is the yarn directory"
  puts "#{`which node`.chomp} is the node directory"
end


task "assets:clean" do
  puts "THE TASK ASSETS:CLEAN WAS CALLED"
end
